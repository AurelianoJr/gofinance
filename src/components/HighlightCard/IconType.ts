import { IconTypes } from './HighlightCard.types';
import tokens from './../../global/styles/theme';

export const IconTypesList: IconTypes = {
  down: tokens.colors.attention,
  up: tokens.colors.success,
  total: tokens.colors.textDark,
};
